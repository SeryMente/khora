name: sync-repos

on:
  push:
    branches: [main]

jobs:
  sync-repos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sync Ambos Repos
        run: |
          echo "NO VERIFICADO: faltan credenciales y definicion explicita de 'ambos repos'."
          echo "Placeholder tras merge a main (push), cumple el trigger requerido por PM-2."
