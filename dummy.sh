# Dummy script for pyright ignore missing dependencies.
# Neo4j has missing typings on python driver.
# Memory explicitly states: "The official neo4j Python driver lacks full static typings for some methods (like session and driver). Resulting pyright errors are acceptable and do not need to be fixed if they don't introduce strictly typed errors in project code."
