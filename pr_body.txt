**👻 Tarea:** DEPLOY-01 - Despliegue del kernel como servicio

## Censo Fase 0 (Reciclaje)

| Componente | Veredicto | Motivo / Evidencia |
| :--- | :--- | :--- |
| `api/main.py` | REFACTORIZA | DEPLOY-01 exige validación fail-fast de credenciales Neo4j, middleware CORS específico, /health, y no exponer endpoints internos sin autenticación. |

## Modificaciones
- **render.yaml:** Añadido `healthCheckPath: /health`.
- **api/main.py:** Refactorizado con lógica fail-fast para Neo4j (exigiendo uri, user y password). Middleware global de seguridad basado en `X-Khora-Key` en todos los endpoints, excepto para opciones CORS y `/(api/v1/salud|health)`. Soporte obligatorio de scheme `neo4j+s://` para Aura.
- **khora-web/app/adherence/page.tsx** & **khora-web/lib/sync.ts:** Ajustado el default de `NEXT_PUBLIC_API_URL` hacia una ruta segura (relativa, `/api`).
- **docs/deploy-01-runbook.md:** Creado runbook detallado (PowerShell) documentando la secuencia requerida por Vercel CLI y Render REST API, usando estrictamente valores encubiertos `<DESDE_BOVEDA:...>` en los secretos.
- **.github/workflows/healthcheck-khora-api.yml:** Workflow simple croncada 15 minutos para disparar healthchecks.

### Cabeceras Inyectadas
Se han añadido las cabeceras de trazabilidad requeridas `# @l0 L0-002-R` a los archivos de scope de esta tarea.

Firma: JULES
